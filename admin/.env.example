# Browser calls the Docker (or local) gateway. Do not use a relative /api —
# Next has no rewrite to the backend.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_token_here
