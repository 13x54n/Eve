import type { Metadata } from "next";
import { Geist_Mono, Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Eve — open-source ride matching",
  description:
    "Community ride-matching marketplace. Riders request, drivers offer fares, payment stays off-platform. No commission. MIT licensed.",
  openGraph: {
    title: "Eve — open-source ride matching",
    description:
      "Self-host community ride matching for bike and car. Inspect matching, fork it, take no platform cut.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-background text-foreground">{children}</body>
    </html>
  );
}
