{
  "name": "eve-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--conditions=development concurrently -n auth,loc,ride,ntfy,admin -c blue,green,magenta,yellow,cyan \"npm run dev -w @eve/auth\" \"npm run dev -w @eve/location\" \"npm run dev -w @eve/ride\" \"npm run dev -w @eve/notify\" \"npm run dev -w @eve/admin\"",
    "dev:split": "npm run dev",
    "build": "prisma generate && tsc -p packages/shared/tsconfig.json && tsc -p packages/db/tsconfig.json && tsc -p packages/http/tsconfig.json && tsc -p services/auth/tsconfig.json && tsc -p services/location/tsconfig.json && tsc -p services/notify/tsconfig.json && tsc -p services/ride/tsconfig.json && tsc -p services/admin/tsconfig.json",
    "clean": "rm -rf packages/*/dist services/*/dist",
    "start": "concurrently -n auth,loc,ride,ntfy,admin \"npm start -w @eve/auth\" \"npm start -w @eve/location\" \"npm start -w @eve/ride\" \"npm start -w @eve/notify\" \"npm start -w @eve/admin\"",
    "test": "vitest run",
    "test:regression": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:contracts": "vitest run tests/contracts",
    "openapi:generate": "tsx scripts/generate-openapi.ts",
    "load:seed": "tsx load/seed-load-users.ts",
    "load:cleanup": "tsx load/cleanup.ts",
    "load:smoke": "k6 run -e BASE_URL=http://localhost:4003 load/health.js && k6 run -e BASE_URL=http://localhost:4003 --vus 5 --duration 20s load/lifecycle.js",
    "load:capacity": "k6 run -e BASE_URL=http://localhost:4003 load/capacity.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:validate": "prisma validate",
    "db:seed": "cross-env NODE_OPTIONS=--conditions=development prisma db seed",
    "db:seed:docker": "docker compose exec auth npm run db:seed",
    "db:reset": "cross-env NODE_OPTIONS=--conditions=development prisma migrate reset --force"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/ms": "^2.1.0",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.5",
    "@types/supertest": "^7.2.1",
    "@types/swagger-jsdoc": "^6.0.4",
    "@vitest/coverage-v8": "^4.1.11",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.1",
    "express-openapi-validator": "^5.4.0",
    "prisma": "^7.9.1",
    "supertest": "^7.2.2",
    "swagger-jsdoc": "^6.2.8",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11"
  }
}
